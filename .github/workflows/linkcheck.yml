# Run sphinx's linkcheck to pop up
# broken and redirected links.

name: Linkcheck

on:
  push:
    paths:
      - '*.diff'
  workflow_dispatch:
    inputs:
      SPHINXOPTS:
        description: 'Sphinx options'
        required: false
        type: string

permissions:
  contents: read

jobs:
  linkcheck:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        branch: ['main', '3.11']
        include:
          - branch: 'main'
            fix-redir-permanently: 'fix-redir-permanently.diff'
            #fix-redir-with-found:
          - branch: '3.11'
            fix-redir-permanently: 'fix-redir-permanently-311.diff'
            #fix-redir-with-found:
      fail-fast: false
    steps:
      - name: Check out this repo
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Check out CPython
        uses: actions/checkout@v3
        with:
          repository: python/cpython
          persist-credentials: false
          ref: ${{ matrix.branch }}
          path: cpython

      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          cache: 'pip'
          cache-dependency-path: 'cpython/Doc/requirements.txt'

      - name: Apply fix-redir-permanently
        if: matrix.fix-redir-permanently != 0
        run: |
          cd cpython
          cat ../${{ matrix.fix-redir-permanently }}
          git apply ../${{ matrix.fix-redir-permanently }}

      - name: Apply fix-redir-with-found
        if: matrix.fix-redir-with-found != 0
        run: |
          cd cpython
          cat ../${{ matrix.fix-redir-with-found }}
          git apply ../${{ matrix.fix-redir-with-found }}
      
      - name: Make virtual environment
        run: |
          make -C cpython/Doc venv

      - name: Run linkcheck
        run: |
          make -C cpython/Doc linkcheck SPHINXOPTS="--keep-going ${{ github.event.inputs.SPHINXOPTS }}" SPHINXERRORHANDLING="" || true

      - name: Split log
        run: |
          BRANCH=${{ matrix.branch }}
          SRCFILE="cpython/Doc/build/linkcheck/output.txt"
          touch ${BRANCH}-log-broken.txt ${BRANCH}-log-redir-permanently.txt ${BRANCH}-log-redir-with-found.txt          
          grep ' \[broken] ' ${SRCFILE} | sort > ${BRANCH}-log-broken.txt || true
          grep ' \[redirected permanently] ' ${SRCFILE} | sort > ${BRANCH}-log-redir-permanently.txt || true
          grep ' \[redirected with Found] ' ${SRCFILE} | sort > ${BRANCH}-log-redir-with-found.txt || true
          sed '/\[broken]/d;/\[redirected with Found]/d;/\[redirected permanently]/d' ${SRCFILE} | sort > ${BRANCH}-log-others.txt
          sort ${SRCFILE} > ${BRANCH}-linkcheck.txt

      - run: cat ${{ matrix.branch }}-log-broken.txt
      - run: cat ${{ matrix.branch }}-log-redir-permanently.txt
      - run: cat ${{ matrix.branch }}-log-redir-with-found.txt
      - run: cat ${{ matrix.branch }}-log-others.txt

      - name: Upload output as artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.branch }}-logs
          path: ${{ matrix.branch }}-*.txt

  push:
    runs-on: ubuntu-latest
    needs: linkcheck
    permissions:
      contents: write
    steps:
      - name: Check out this repo
        uses: actions/checkout@v3
      
      - name: Download logs
        uses: actions/download-artifact@v3
        
      - name: Commit and push logs
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git status
          git add *-logs/*.txt
          git commit --allow-empty -m "Update linkcheck log" -m "SPHINXOPTS used: '--keep-going ${{ github.event.inputs.SPHINXOPTS }}'"
          git pull --rebase
          git push
